# App for Agentic Demo on Frontier
Specify `emb` in the launch script for different model sizes, and the code will look for corresponding configurations in `config/ViT.yaml` 
```bash
sbatch submit_frontier.sh
```
